export class User {
  // user access token
  token: string;
  // user Id
  userId: string;
  // user refresh token
  refreshToken: string;
  // user email
  email: string;
  // user first name
  first: string;
  // user last name
  last: string;
}
