export class Convertedsize {
  label: string;
  displayLabel: string;
  quantity: number;

  public constructor(label: string, displayLabel: string, quantity: number) {
    this.label = label;
    this.displayLabel = displayLabel;
    this.quantity = quantity;
  }
}
